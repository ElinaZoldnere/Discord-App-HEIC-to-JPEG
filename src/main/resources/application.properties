# For local development:
# Load environment variables from .env file if present (optional)
spring.config.import=optional:file:.env[.properties]

# For deployment:
# Environment variables are specified in app.yaml on Google App Engine

# Environment variables used in this application (list for reference only):
# DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN}
# OWNER_ID=${OWNER_ID}
# CLOUD_FUNCTION_URL=${CLOUD_FUNCTION_URL}
# SECRET_TOKEN=${SECRET_TOKEN}